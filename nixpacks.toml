[variables]
NIXPACKS_PYTHON_VERSION = '3.10'

[phases.setup]
nixPkgs = ['gcc']

[phases.install]
cmds = ['pip install -r requirements.txt']

[start]
cmd = 'cd backend && alembic upgrade head && uvicorn api:app --host 0.0.0.0 --port $PORT --workers 2 --proxy-headers --forwarded-allow-ips="*"'
